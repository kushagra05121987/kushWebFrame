<div class="alert alert-danger">
    <div class="alert-title">{{ $title }}</div>
    Tripple Braces Escaped {{{ $name }}}
    Single Brace Escaped {!! $name !!}
    Double Braces Escaped {{ $slot }}
</div>